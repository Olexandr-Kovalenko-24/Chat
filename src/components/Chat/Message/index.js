import React from 'react';

const Message = (props) => {
    const {message: {body, id, postId, user}} = props.message;
    return (
        <div>
            
        </div>
    );
}

export default Message;
